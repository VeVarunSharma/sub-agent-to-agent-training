# Infra (Bicep + azd)

Stub. The real infra lands in `p5-bicep` and `p5-deploy`.

The Foundry project, the Azure OpenAI deployment that hosts the judge model (`srs-judge-gpt-4-1-20250414`), the storage account, Cosmos, and the Next.js host live here.
