#!/usr/bin/env -S node --experimental-strip-types

console.error(
  [
    "scripts/validate-data.ts is a stub. Implementation lands in p1-validator.",
    "Contract: see specs/002-synthetic-data/SPEC.md `Validator` section (19 assertions).",
  ].join("\n"),
);
process.exit(2);
