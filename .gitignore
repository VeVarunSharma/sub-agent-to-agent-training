# dependencies
node_modules/
.pnpm-store/

# build outputs
.next/
out/
dist/
build/
*.tsbuildinfo
.turbo/

# next.js
.next/
next-env.d.ts

# environment
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*

# coverage
coverage/
*.lcov

# vercel
.vercel/

# editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo

# os
.DS_Store
Thumbs.db

# session + scratch
*.local
.srs-iterate-tmp/

# eval scratch (rounds live here once produced)
eval-reports/*/scratch/

# secrets (sealed cases stay encrypted in repo; keys never enter the tree)
*.key
!*.age.key.pub
# Squad: ignore runtime state (logs, inbox, sessions)
.squad/orchestration-log/
.squad/log/
.squad/decisions/inbox/
.squad/sessions/
.squad/.scratch/
.squad/.cache/
# Squad: SubSquad activation file (local to this machine)
.squad-workstream

# Sealed-only datasets. The .age versions are committed; plaintext stays local.
datasets/cases/van-ssmuh.holdout.jsonl
datasets/cases/van-ssmuh.gold-holdout.jsonl
